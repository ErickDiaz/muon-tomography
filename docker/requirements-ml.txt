# Dependencias ML adicionales para thesis-ml (Fase 3).
# PyTorch/torchvision vienen en la imagen base — no instalar aqui.

# Notebooks (no vienen en requirements.txt base: es un grupo opcional de
# poetry que `export-requirements` no exporta)
jupyterlab>=4.0
ipykernel>=6.27

# Tracking de experimentos
mlflow>=2.14
tensorboard>=2.16
wandb>=0.17

# Modelos / utilidades
einops>=0.8
torchmetrics>=1.4
lightning>=2.4         # para entrenamiento estructurado (opcional)

# Augmentaciones para muogramas (Opcion B: super-resolucion)
albumentations>=1.4
opencv-python-headless>=4.10

# Hyperparameter tuning (opcional)
optuna>=3.6
